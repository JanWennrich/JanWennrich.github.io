{
  "devDependencies": {
    "critical": "^1.3.10",
    "purgecss": "^2.3.0"
  },
  "scripts": {
    "purge-css": "purgecss --config assets/purgecss/config.js --output css/purged",
    "critical-css": "cat index.html | critical --inline > index.critical.html"
  }
}
